{
  "name": "coresound-radio-stream-engine",
  "version": "2.0.0-stage1",
  "private": true,
  "type": "module",
  "description": "CoreSound Radio Stream Engine V2 - HLS worker using CoreSound /api/radio/now-playing as source of truth.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development node src/server.js",
    "check": "node src/check-env.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
